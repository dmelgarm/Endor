{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://endor.dev/schema/logic-tree.schema.json",
  "title": "Endor PTHA Logic Tree",
  "description": "A weighted epistemic-uncertainty logic tree for probabilistic tsunami hazard analysis. Each node is a branch point over one parameter; each branch carries a weight and an optional child node. A root-to-leaf path is one model realization whose weight is the product of its branch weights.",
  "type": "object",
  "required": ["schemaVersion", "tree"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "type": "string",
      "description": "Semantic version of this logic-tree format.",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "metadata": {
      "type": "object",
      "description": "Free-form descriptive information about the tree.",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "author": { "type": "string" },
        "created": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": true
    },
    "naming": {
      "type": "object",
      "description": "How to assemble a rupture name from the `code`s along a root-to-leaf path.",
      "additionalProperties": false,
      "properties": {
        "separator": {
          "type": "string",
          "description": "Joins consecutive branch codes. Defaults to '-'."
        },
        "prefix": {
          "type": "string",
          "description": "Prepended to every rupture name."
        },
        "suffix": {
          "type": "string",
          "description": "Appended to every rupture name, e.g. '-result_trimmed'."
        }
      }
    },
    "tree": { "$ref": "#/$defs/node" }
  },
  "$defs": {
    "node": {
      "type": "object",
      "description": "A branch point: a single epistemic choice over `parameter`, expanded into mutually exclusive, collectively exhaustive branches.",
      "required": ["parameter", "branches"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Optional stable identifier. Auto-generated by tools when absent."
        },
        "parameter": {
          "type": "string",
          "description": "The quantity this node makes an epistemic choice about, e.g. 'scaling_relation'."
        },
        "label": {
          "type": "string",
          "description": "Human-readable display name for the branch point."
        },
        "branches": {
          "type": "array",
          "description": "Mutually exclusive alternatives. Weights should sum to 1.",
          "minItems": 1,
          "items": { "$ref": "#/$defs/branch" }
        }
      }
    },
    "branch": {
      "type": "object",
      "description": "One alternative of a branch point. A branch with no child `node` is a leaf.",
      "required": ["weight"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Optional stable identifier. Auto-generated by tools when absent."
        },
        "label": {
          "type": "string",
          "description": "Human-readable name for this alternative."
        },
        "code": {
          "type": "string",
          "description": "Token for this choice in the rupture-naming convention. The rupture name of a leaf is the ordered join of the `code`s along its root-to-leaf path. Branches without a code contribute nothing to the name."
        },
        "weight": {
          "type": "number",
          "description": "Epistemic weight (probability) of this alternative among its siblings.",
          "minimum": 0,
          "maximum": 1
        },
        "value": {
          "description": "The parameter value this branch selects. Any JSON type, including objects for structured parameters."
        },
        "node": {
          "description": "Optional child branch point. Absent means this branch is a leaf.",
          "$ref": "#/$defs/node"
        }
      }
    }
  }
}
